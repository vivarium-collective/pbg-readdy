"""Custom bigraph-schema types for ReaDDy wrapper."""


def register_readdy_types(core):
    """Register custom types used by ReaDDy processes.

    Currently uses built-in types only. This function is a hook for
    future custom type registrations (e.g. topology types, unit-bearing
    quantities).
    """
    pass
